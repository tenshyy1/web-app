### Backend
1. Navigate to the `server` directory:
   ```bash
   cd server
   npm install ws
   start: node server.js
   ```

### client
2. Navigate to the `client` directory:
    ```bash
    cd client
    npm install
    start: npm start
    ```

### admin
2. Navigate to the `client` directory:
    ```bash
    cd admin
    npm install
    start: npm start